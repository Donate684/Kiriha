<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vmPlayer="using:Kiriha.ViewModels.Player"
             xmlns:mpv="clr-namespace:Kiriha.Mpv"
             xmlns:material="using:Material.Icons.Avalonia"
             xmlns:conv="using:Kiriha.Views.Converters"
             x:Class="Kiriha.Views.Player.Controls.PlayerSettingsOverlay"
             x:DataType="vmPlayer:PlayerViewModel"
             xmlns:settings="using:Kiriha.Views.Player.Controls.Settings"
             IsVisible="False">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceInclude Source="avares://Kiriha/Views/Styles/PlayerOverlayWindowResources.axaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
        <Border Name="SettingsOverlayBackdrop"
                Background="#33000000"
                PointerPressed="OnSettingsBackdropPointerPressed">
            <Border Name="SettingsOverlayPanel"
                    Width="760"
                    MaxWidth="760"
                    MaxHeight="520"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    CornerRadius="8"
                    Background="#F01A1A1A"
                    BorderBrush="#55FFFFFF"
                    BorderThickness="1"
                    ClipToBounds="True"
                    PointerPressed="OnSettingsPanelPointerPressed">
                <Grid ColumnDefinitions="196,*">
                    <Border Grid.Column="0"
                            Background="#18000000"
                            BorderBrush="#22FFFFFF"
                            BorderThickness="0,0,1,0"
                            Padding="0,12">
                        <DockPanel LastChildFill="True">
                            <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto" Margin="18,8,12,14">
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{DynamicResource l.player.title}"
                                               Foreground="White"
                                               FontSize="16"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="{DynamicResource l.player.settings}"
                                               Foreground="#80FFFFFF"
                                               FontSize="11"/>
                                </StackPanel>
                            </Grid>

                            <Border DockPanel.Dock="Bottom" Margin="12,0,12,14" Background="#10FFFFFF" CornerRadius="4" Padding="10" BorderBrush="#20FFFFFF" BorderThickness="1">
                                <StackPanel Spacing="3">
                                    <TextBlock Text="{Binding MpvRuntimeInfo.ResolutionFps}" 
                                               TextWrapping="Wrap"
                                               FontFamily="Consolas,Courier New,monospace" FontSize="10" Foreground="#80FFFFFF"
                                               IsVisible="{Binding MpvRuntimeInfo.ResolutionFps, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                               
                                    <TextBlock Text="{Binding MpvRuntimeInfo.CodecDecoder}" 
                                               TextWrapping="Wrap"
                                               FontFamily="Consolas,Courier New,monospace" FontSize="10" Foreground="#80FFFFFF"
                                               IsVisible="{Binding MpvRuntimeInfo.CodecDecoder, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                               
                                    <TextBlock Text="{Binding MpvRuntimeInfo.HwDecVo}" 
                                               TextWrapping="Wrap"
                                               FontFamily="Consolas,Courier New,monospace" FontSize="10"
                                               IsVisible="{Binding MpvRuntimeInfo.HwDecVo, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                                        <TextBlock.Tag>
                                            <Binding Path="MpvRuntimeInfo.IsHwDecActive"/>
                                        </TextBlock.Tag>
                                        <TextBlock.Styles>
                                            <Style Selector="TextBlock">
                                                <Setter Property="Foreground" Value="#80FFFFFF"/>
                                            </Style>
                                            <Style Selector="TextBlock[Tag=True]">
                                                <Setter Property="Foreground" Value="#FF4CAF50"/>
                                            </Style>
                                        </TextBlock.Styles>
                                    </TextBlock>
                                    
                                    <TextBlock Text="{Binding MpvRuntimeInfo.Drops}" 
                                               TextWrapping="Wrap"
                                               FontFamily="Consolas,Courier New,monospace" FontSize="10" Foreground="#80FFFFFF"
                                               IsVisible="{Binding MpvRuntimeInfo.Drops, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                </StackPanel>
                            </Border>

                            <ListBox Name="CenteredPlayerSettingsNav"
                                     Classes="player-settings-nav"
                                     SelectedIndex="0">
                                <ListBoxItem>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <material:MaterialIcon Kind="PlayCircleOutline" Width="17" Height="17" Foreground="White"/>
                                        <TextBlock Text="{DynamicResource l.player.playback}" Foreground="White" FontSize="12"/>
                                    </StackPanel>
                                </ListBoxItem>
                                <ListBoxItem>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <material:MaterialIcon Kind="VolumeHigh" Width="17" Height="17" Foreground="White"/>
                                        <TextBlock Text="{DynamicResource l.player.audio}" Foreground="White" FontSize="12"/>
                                    </StackPanel>
                                </ListBoxItem>
                                <ListBoxItem>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <material:MaterialIcon Kind="ViewDashboardOutline" Width="17" Height="17" Foreground="White"/>
                                        <TextBlock Text="{DynamicResource l.player.interface}" Foreground="White" FontSize="12"/>
                                    </StackPanel>
                                </ListBoxItem>
                                <ListBoxItem>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <material:MaterialIcon Kind="SubtitlesOutline" Width="17" Height="17" Foreground="White"/>
                                        <TextBlock Text="{DynamicResource l.player.tracks}" Foreground="White" FontSize="12"/>
                                    </StackPanel>
                                </ListBoxItem>
                                <ListBoxItem>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <material:MaterialIcon Kind="VideoOutline" Width="17" Height="17" Foreground="White"/>
                                        <TextBlock Text="{DynamicResource l.settings.player.video_preset_title}" Foreground="White" FontSize="12"/>
                                    </StackPanel>
                                </ListBoxItem>
                                <ListBoxItem>
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <material:MaterialIcon Kind="Tune" Width="17" Height="17" Foreground="White"/>
                                        <TextBlock Text="{DynamicResource l.player.advanced}" Foreground="White" FontSize="12"/>
                                    </StackPanel>
                                </ListBoxItem>
                            </ListBox>
                        </DockPanel>
                    </Border>

                    <ScrollViewer Grid.Column="1"
                                  Padding="24,22"
                                  HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Auto">
                        <Panel>
                            <settings:PlayerPlaybackSettings IsVisible="{Binding ElementName=CenteredPlayerSettingsNav, Path=SelectedIndex, Converter={StaticResource IntEqualsConverter}, ConverterParameter=0}"/>
                            <settings:PlayerAudioSettings IsVisible="{Binding ElementName=CenteredPlayerSettingsNav, Path=SelectedIndex, Converter={StaticResource IntEqualsConverter}, ConverterParameter=1}"/>
                            <settings:PlayerInterfaceSettings IsVisible="{Binding ElementName=CenteredPlayerSettingsNav, Path=SelectedIndex, Converter={StaticResource IntEqualsConverter}, ConverterParameter=2}"/>
                            <settings:PlayerTrackSettings IsVisible="{Binding ElementName=CenteredPlayerSettingsNav, Path=SelectedIndex, Converter={StaticResource IntEqualsConverter}, ConverterParameter=3}"/>
                            <settings:PlayerVideoSettings IsVisible="{Binding ElementName=CenteredPlayerSettingsNav, Path=SelectedIndex, Converter={StaticResource IntEqualsConverter}, ConverterParameter=4}"/>
                            <settings:PlayerAdvancedSettings IsVisible="{Binding ElementName=CenteredPlayerSettingsNav, Path=SelectedIndex, Converter={StaticResource IntEqualsConverter}, ConverterParameter=5}"/>
                        </Panel>
                    </ScrollViewer>
                    <Button Grid.Column="1"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Classes="icon-btn"
                            Padding="8"
                            Margin="0,14,18,0"
                            Click="OnSettingsCloseClick">
                        <material:MaterialIcon Kind="Close" Width="18" Height="18" Foreground="White"/>
                    </Button>
                </Grid>
            </Border>
        </Border>
</UserControl>

